<?xml version="1.0" encoding="UTF-8"?>
<configuration>

	<props>
		<prop name="mysql.driver" value="com.mysql.cj.jdbc.Driver" />
		<prop name="mysql.url"
			value="jdbc:mysql://localhost:3306/test?useUnicode=true&amp;characterEncoding=utf8&amp;serverTimezone=UTC" />
		<prop name="mysql.username" value="root" />
		<prop name="mysql.password" value="123" />
	</props>

	<sessionFactories>
		<sessionFactory id="development1">
			<transactionManager type="JDBC">
				<property name="" value=""/>
			</transactionManager>
			<dataSource id="">
				<property name="driver" value="${mysql.driver}" />
				<property name="url" value="${mysql.url}" />
				<property name="username" value="${mysql.username}" />
				<property name="password" value="${mysql.password}" />
			</dataSource>
		</sessionFactory>

		<sessionFactory id="development2">
			<dataSource>
				<property name="driver" value="${mysql.driver}" />
				<property name="url" value="${mysql.url}" />
				<property name="username" value="${mysql.username}" />
				<property name="password" value="${mysql.password}" />
			</dataSource>
		</sessionFactory>
	</sessionFactories>





</configuration>